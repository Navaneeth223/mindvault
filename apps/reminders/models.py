"""
Reminders are stored directly on Card model (reminder_at, reminder_done fields).
This file exists for app structure but doesn't define additional models.
"""
# No additional models needed — reminders are part of Card
